version: '1'
name: phpenv
services:
  nginx:
    image: 'nginx:stable'
    privileged: true
    volumes:
      - ./etc/nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      - ./etc/nginx/conf.d:/etc/nginx/conf.d:ro
      - ./etc/nginx/cert:/etc/nginx/cert:ro
      - ./var/log/nginx:/var/log/nginx:rw
      - $WORKSPACE_DIR/www:/usr/share/nginx/html:rw
    depends_on:
      - php7
      - php8
    networks:
      - dev
    restart: always
    environment:
      - TZ=Asia/Shanghai
 
  php7:
    image: 'php:my-7.4-fpm'
    privileged: true
    volumes:
      - ./etc/php/php.ini:/usr/local/etc/php/php.ini:ro
      - ./etc/php/php-fpm.conf:/usr/local/etc/php-fpm.conf:ro
      - ./etc/php/www.conf:/usr/local/etc/php-fpm.d/www.conf:ro
      - ./var/log/php/7:/var/log/php:rw
      - $WORKSPACE_DIR/www:/www:rw
    external_links:
      - skywalking-oap-server-1:skywalking-oap
      - storage-redis-1:redis
      - storage-mysql-1:mysql
    networks:
      - dev
    restart: always
    environment:
      - TZ=Asia/Shanghai

  php8:
    image: 'php:my-8.2-fpm'
    privileged: true
    volumes:
      - ./etc/php/php.ini:/usr/local/etc/php/php.ini:ro
      - ./etc/php/php-fpm.conf:/usr/local/etc/php-fpm.conf:ro
      - ./etc/php/www.conf:/usr/local/etc/php-fpm.d/www.conf:ro
      - ./var/log/php/8:/var/log/php:rw
      - $WORKSPACE_DIR/www:/www:rw
    external_links:
      - storage-redis-1:redis
      - storage-mysql-1:mysql
    networks:
      - dev
    restart: always
    environment:
      - TZ=Asia/Shanghai
 
networks:
  dev:
    external: true
